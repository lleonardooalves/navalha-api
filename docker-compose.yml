services:
  db:
    image: postgres:16
    container_name: navalha-db
    environment:
      POSTGRES_USER: navalha
      POSTGRES_PASSWORD: navalha
      POSTGRES_DB: navalha
    ports:
      - "5432:5432"
    volumes:
      - navalha-data:/var/lib/postgresql/data

volumes:
  navalha-data: